# -*- coding: utf-8 -*-
"""16개 구도 정의 (번호, 라벨) - "generate ppt.py" 프로토타입에서 그대로 추출."""

COMPOS = [
    (1, "전면_전신(1)"),
    (2, "전면_전신(2)"),
    (3, "전면_체간"),
    (4, "전면_상반신(1)"),
    (5, "전면_상반신(2)"),
    (6, "전면_하반신"),
    (7, "측면_전신"),
    (8, "측면_체간"),
    (9, "측면_상반신"),
    (10, "측면_하반신"),
    (11, "후면_전신(1)"),
    (12, "후면_전신(2)"),
    (13, "후면_체간"),
    (14, "후면_상반신(1)"),
    (15, "후면_상반신(2)"),
    (16, "후면_하반신"),
]
WIDE_COMPOS = {5, 15}  # 유일하게 가로로 긴 구도 (팔벌림)

_LABEL_BY_ID = dict(COMPOS)


def label_for(compos_id: int) -> str:
    return _LABEL_BY_ID.get(compos_id, f"미지정({compos_id})")
