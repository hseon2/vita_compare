# 한의원 다이어트 전-후 PPT 자동화 솔루션 — 요구사항 정의서

버전: 0.1 (초안) · 작성일: 2026-08-30

---

## 1. 프로젝트 개요

한의원 다이어트 환자의 전(Before)-후(After) 비교 사진을 업로드하면, 구도 자동 분류 → 수평 조정 → 크롭 → PPT 자동 생성까지 처리하는 로컬 자동화 파이프라인.

- **AI API 미사용**: MediaPipe, OpenCV 등 로컬 실행 오픈소스 라이브러리만 사용 (클라우드/유료 AI API 호출 없음)
- **완전 로컬 처리**: 사진 업로드부터 PPT 생성까지 외부 서버 전송 없음
- **저장 방식**: 로컬 파일시스템 (클라우드 연동 없음)

---

## 2. 기술 스택

| 영역 | 기술 |
|---|---|
| 포즈 검출 | MediaPipe Pose |
| 이미지 처리 | OpenCV, Pillow |
| PPT 생성 | python-pptx |
| 백엔드 API | FastAPI (전처리/분류/크롭/생성 로직을 API로 노출) |
| 프론트엔드 UI | React + TypeScript (Vite) |
| 상태관리 | React Query(서버상태) + Zustand(화면 로컬상태) |
| 스타일 | Tailwind CSS |
| 언어 | Python 3.10+ (백엔드), Node 18+ (프론트엔드) |
| 패키지 관리 | venv + requirements.txt (백엔드), npm (프론트엔드) |

> 백엔드와 프론트엔드를 분리해 로컬 웹앱 형태로 구동. 백엔드는 로컬호스트에서만 서비스(외부 노출 없음), 프론트는 브라우저에서 접속하는 검수/생성 도구.

---

## 3. 레포지토리 구조 (제안)

```
diet-ppt-automation/
├── backend/
│   ├── api/
│   │   ├── main.py                # FastAPI 앱 진입점, 라우터 등록
│   │   └── routes/
│   │       ├── upload.py          # 사진 업로드 엔드포인트
│   │       ├── classify.py        # 구도 자동분류 엔드포인트
│   │       ├── crop.py            # 수평조정/크롭 엔드포인트
│   │       └── generate.py        # PPT 생성 엔드포인트
│   ├── preprocessing/
│   │   ├── pose_detector.py       # MediaPipe 랜드마크 추출
│   │   ├── classifier.py          # 구도 자동 분류
│   │   ├── leveler.py             # 수평 조정
│   │   └── cropper.py             # 크롭 (가이드선/투명가이드 포함)
│   ├── ppt_generator/
│   │   ├── generate_ppt.py        # PPT 생성 메인 로직
│   │   └── compos.py              # 16개 구도 정의 (라벨, wide 여부 등)
│   ├── models/
│   │   └── schema.py              # 데이터 모델 (pydantic/dataclass)
│   ├── config.py                  # 경로, 기본 크롭 사이즈 등 설정값
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── UploadPage.tsx         # 화면1: 업로드 + 환자정보 + 기간모드
│   │   │   ├── ClassifyReviewPage.tsx # 화면2: 구도분류 확인/재지정
│   │   │   ├── CropAdjustPage.tsx     # 화면3: 수평조정 + 크롭
│   │   │   ├── MatchConfirmPage.tsx   # 화면4: 전-후 매칭 최종확인
│   │   │   └── GeneratePage.tsx       # 화면5: 생성 + 진행상태 + 완료
│   │   ├── components/
│   │   │   ├── PhotoThumbnail.tsx
│   │   │   ├── ConfidenceBadge.tsx    # 낮은 신뢰도 하이라이트
│   │   │   ├── CropCanvas.tsx         # 크롭박스 드래그 + 보조선 + 가이드토글
│   │   │   ├── AngleSlider.tsx        # 수평조정 슬라이더
│   │   │   └── BodyCompForm.tsx       # 체성분 수치 입력폼
│   │   ├── api/
│   │   │   └── client.ts              # 백엔드 API 호출 래퍼
│   │   ├── store/                     # Zustand 전역 상태 (세션/모드/사진목록)
│   │   └── App.tsx                    # 라우팅
│   ├── package.json
│   └── vite.config.ts
├── output/{환자명}/                # 최종 PPT 저장
├── source/{환자명}/                # 원본/크롭본 저장
├── tests/
└── README.md
```

---

## 4. 데이터 모델

```python
@dataclass
class Patient:
    name: str
    patient_id: str  # 내부 식별자 (파일명 충돌 방지용, 이름과 별도)

@dataclass
class ShootSession:
    session_date: date          # 촬영일 (시작/중간/마지막 중 하나)
    session_type: str           # "start" | "mid" | "end"
    photos: dict[int, "PhotoAsset"]  # key = 구도 번호(1~16)

@dataclass
class PhotoAsset:
    compos_id: int               # 1~16
    raw_path: str                 # 원본 업로드 경로
    rotation_deg: float           # 적용된 수평 보정 각도
    crop_box: tuple[int,int,int,int]  # 최종 크롭 좌표 (x0,y0,x1,y1)
    cropped_path: str             # 크롭 완료 이미지 경로
    classification_confidence: float  # 자동분류 신뢰도 (0~1)
    manually_confirmed: bool      # 검수 UI에서 사람이 확인했는지

@dataclass
class PPTJob:
    patient: Patient
    mode: str                     # "standard" | "long"
    sessions: list[ShootSession]  # standard=2개(start,end), long=3개(start,mid,end)
    body_comp_rows: list["BodyCompRow"]
    output_path: str

@dataclass
class BodyCompRow:
    label: str            # 예: "복부둘레"
    start: str             # 예: "96.1cm" (단위 포함 문자열로 저장)
    mid: str | None        # 장기모드에서만 값, 표준모드는 None(빈칸으로 표시)
    end: str
    target: str            # 목표치(적정치), 범위 표현 가능 (예: "18~28%")
    highlight: bool         # True면 PPT에서 항목명+수치를 빨간 글씨로 표시
    # 변화량은 저장하지 않고 PPT 생성 시 start/end로 자동 계산 (단위 자동 매칭)
```

---

## 5. 모듈별 상세 요구사항

### 5.1 전처리 — 포즈 검출 (`pose_detector.py`)

- **입력**: 이미지 파일 경로 (jpg/png)
- **처리**: MediaPipe Pose로 33개 랜드마크 좌표(x,y,z,visibility) 추출
- **출력**: `dict[str, tuple[float,float,float]]` 형태의 랜드마크 좌표 (정규화 좌표 0~1)
- **예외 처리**: 인물이 검출되지 않으면 `PoseNotDetectedError` 발생 → 검수 UI에서 수동 처리 유도

### 5.2 전처리 — 구도 자동 분류 (`classifier.py`)

- **입력**: 랜드마크 좌표
- **분류 기준** (16개 구도, 번호는 `compos.py` COMPOS와 일치):

| 구분 기준 | 사용 랜드마크 |
|---|---|
| 정면/측면/후면 | 얼굴 랜드마크 유무, 어깨-골반 폭 비율 |
| 전신/상반신/하반신 | 발목·무릎·어깨 랜드마크 존재 범위 |
| 다리 벌림/오므림 | 발목 간 거리 (어깨너비 대비 비율) |
| 팔벌림(구도5, 15) | 팔꿈치-어깨 각도 (수평에 가까운지) |

- **출력**: `(compos_id: int, confidence: float)` — confidence는 0~1
- **신뢰도 임계값**: `confidence < 0.7`이면 검수 UI에서 "확인 필요" 배지 표시 (자동 확정 아님)
- **미해결 이슈**: 정면 vs 후면 구분은 얼굴 랜드마크 신뢰도에 크게 의존 → 오분류 시 검수 UI에서 수동 재지정 가능해야 함

### 5.3 전처리 — 수평 조정 (`leveler.py`)

- **입력**: 이미지, 랜드마크 좌표
- **처리**: 눈-눈 라인 또는 어깨 라인의 기울기(atan2) 계산 → 반대 방향으로 이미지 회전
- **출력**: 회전된 이미지, 적용 각도(`rotation_deg`)
- **UI 연동 요건**: 검수 화면에서 자동 계산된 각도를 슬라이더로 미세조정 가능해야 함 (수동 오버라이드)

### 5.4 전처리 — 크롭 (`cropper.py`)

**핵심 원칙: AI 1차 자동크롭 → 사람 확정/수정.** AI가 랜드마크 기반으로 크롭박스를 먼저 계산해 제안하고, 사람이 그대로 확정하거나 추가로 조정한다. 크롭은 항상 **비파괴적**으로 처리한다 — 원본(`raw_path`)은 그대로 보존하고 `crop_box` 좌표만 저장/갱신하므로, 사람이 크롭 범위를 넓히거나 좁히거나 다시 되돌리는 작업이 언제든 원본 기준으로 가능하다 (한번 잘라낸 뒤 다시 넓힐 수 없는 구조 금지).

- **디폴트 크롭 사이즈**: 구도별 고정 비율 (예: 전신 3:4, 팔벌림 구도는 16:9 계열) — `config.py`에 상수로 정의, AI 1차 크롭의 기준값으로 사용
- **AI 1차 자동크롭 로직**: 랜드마크(어깨/골반/무릎/발목 등)로 구도별 기준점을 찾아 디폴트 사이즈 내에서 인물이 중앙에 오도록 `crop_box` 자동 계산
- **사람 수정 범위**: 크롭박스를 드래그로 넓히기/좁히기/이동, 수평각도 재조정 — 모두 원본 이미지 위에서 다시 계산 (AI 결과를 지우고 새로 하는 게 아니라 AI가 제안한 박스를 시작점으로 조정)
- **좌우 동기화**: 사용자가 크기를 변경하면 전/후 사진 모두 동일한 사이즈로 적용
- **가이드 오버레이**:
  - 보조선(수평 기준선) 항상 노출
  - 투명 이미지 가이드 토글 가능 (표시/숨김)
  - 하단에 텍스트로 가이드 문구 노출 (예: "아랫입술 아래 ~ 무릎뼈 아래")
- **출력**: `crop_box` 좌표 저장 (픽셀을 실제로 잘라내 별도 파일로 저장하는 건 최종 확정 시점에만 수행 — 그 전까지는 원본+좌표만 유지)
- **상태 플래그**: `PhotoAsset.manually_confirmed` — AI 제안을 그대로 쓴 건지 사람이 수정했는지 구분 (QA/로그용)

### 5.5 프론트엔드 UI + 백엔드 API

프론트(React)와 백엔드(FastAPI)를 분리하고, 아래 API 계약으로 통신한다. 모든 요청은 `localhost` 내부에서만 오가며 외부로 노출되지 않는다.

#### 5.5.1 화면 구성 (5단계 위저드)

| # | 화면 | 주요 기능 |
|---|---|---|
| 1 | 업로드 (`UploadPage`) | 환자명 입력, 기간모드(표준/장기) 토글, 세션별(시작/중간/마지막) 사진 업로드. 장기모드 선택 시 중간날짜 업로드 슬롯이 추가로 나타남 |
| 2 | 분류 확인 (`ClassifyReviewPage`) | 구도별 썸네일 그리드 + 자동분류 결과 표시. `confidence < 0.7`인 항목은 `ConfidenceBadge`로 강조, 드롭다운으로 구도 재지정 가능 |
| 3 | 수평/크롭 조정 (`CropAdjustPage`) | **AI가 1차 자동크롭한 결과를 먼저 보여주고**, 사람이 그대로 확정하거나 수정. `AngleSlider`로 회전각 미세조정, `CropCanvas`에서 크롭박스 드래그(좌우 동기화) — 넓히기/좁히기/이동 모두 원본 기준으로 비파괴적 처리, 보조선 상시 노출, 투명가이드 토글, 하단 텍스트 가이드 노출 |
| 4 | 전-후 매칭 확인 (`MatchConfirmPage`) | 동일 구도의 시작/중간/마지막 사진이 올바르게 짝지어졌는지 좌우 비교 뷰로 최종 확인, `BodyCompForm`으로 체성분 수치 입력 — 항목별 "강조" 체크박스 포함(체크 시 PPT에서 빨간 글씨로 표시), 변화량은 입력 없이 자동 계산되어 미리보기로만 표시 |
| 5 | 생성 (`GeneratePage`) | "PPT 생성" 버튼 → 진행률 표시(폴링 또는 SSE) → 완료 시 `output_path` 안내 + 파일 다운로드 링크 |

- **공통 UX 요건**
- 5단계 모두 상단에 진행 스텝 인디케이터 표시, 이전 단계로 자유롭게 돌아가기 가능
- 새로고침 시에도 진행 중이던 세션 유지 — DB가 아닌 **서버 메모리(또는 임시 JSON 파일 1개)**로 충분. 생성 완료 후 해당 세션 상태는 폐기해도 무방 (환자 이력 조회/검색 기능은 이 서비스 범위 밖)

#### 5.5.2 API 엔드포인트 (초안)

| Method | Endpoint | 설명 |
|---|---|---|
| POST | `/api/sessions` | 새 작업 세션 생성 (환자명, 모드) → `session_id` 반환 |
| POST | `/api/sessions/{id}/photos` | 사진 업로드 (세션타입: start/mid/end) |
| POST | `/api/sessions/{id}/classify` | 업로드된 사진 전체 자동 구도분류 실행 |
| PATCH | `/api/sessions/{id}/photos/{photo_id}` | 구도 재지정, 회전각 수정, 크롭박스 수정 |
| GET | `/api/sessions/{id}/photos` | 현재 세션의 사진/분류/크롭 상태 조회 |
| POST | `/api/sessions/{id}/body-comp` | 체성분 수치 저장 |
| POST | `/api/sessions/{id}/generate` | PPT 생성 트리거 (비동기 작업 시작) |
| GET | `/api/sessions/{id}/generate/status` | 생성 진행상태 조회 (폴링용) |
| GET | `/api/sessions/{id}/download` | 완성된 PPT 다운로드 |

- 모든 응답은 JSON, 사진 관련 응답에는 썸네일 URL(`/static/...`) 포함
- 에러 응답 포맷 통일: `{ "error_code": "...", "message": "..." }` (예: `POSE_NOT_DETECTED`, `LOW_CONFIDENCE`)

### 5.6 PPT 생성 (`ppt_generator/generate_ppt.py`)

*(이미 프로토타입 구현 완료 — 아래는 확정된 사양)*

- **슬라이드 순서**: 구도1(세트1) → 구도1(세트2) → 구도2(세트1) → … → 구도16(세트2) → 체성분표
  - 표준모드: 16슬라이드 + 체성분표 1 = 17슬라이드
  - 장기모드: 32슬라이드 + 체성분표 1 = 33슬라이드
- **레이아웃**: 좌측상단 구도 텍스트박스(`{번호}. {라벨}`), 사진 좌상단 전/후 태그
  - **일반 구도(1~4, 6~14, 16번)**: 전/후 사진 좌우 배치. 사진마다 원본 비율을 유지한 실제 크기를 먼저 계산한 뒤, 그 크기끼리 0.2인치 간격으로 딱 붙여 슬라이드 중앙에 배치 — 박스를 넓게 잡고 안에서 비율유지만 하면 세로형 사진 특성상 양옆에 빈 여백이 커져 사진 사이가 떨어져 보이므로, 실제 렌더 크기 기준으로 붙이는 방식을 사용
  - **wide 구도(5, 15번 — 팔벌림)**: 좌우 배치가 아닌 **대각선 배치**. 전 사진은 좌측상단, 후 사진은 우측하단에 배치 (`WIDE_COMPOS` 상수로 판별). 각 사진은 가용 폭의 52%, 가용 높이의 72% 박스 안에서 비율유지 — 대각선 어긋남이 뚜렷이 보이도록 서로 겹치지 않는 크기로 조정됨
- **날짜 표기**: 덱 전체에서 **첫 번째 슬라이드에만** 사진 하단 날짜 캡션 표시
- **파일명 규칙**: `{환자명}님_프레젠테이션_{생성일자:YYYYMMDD}.pptx`
- **저장 경로**: `/output/{환자명}/{파일명}`
- **체성분 표 (마지막 슬라이드) 상세 사양**: *(실제 병원 양식 기준으로 확정됨 — 참고: `IMG_4290.jpeg`)*
  - 컬럼: `항목 | 시작 | 중간 | 마지막 | 변화량 | 목표치(적정치)` — 표준모드에서도 "중간" 컬럼은 제거하지 않고 빈칸으로 유지
  - **강조(하이라이트) 항목**: `BodyCompRow.highlight = True`인 행은 항목명+모든 수치 셀을 빨간 글씨로 표시 (예: 복부둘레·엉덩이둘레·가슴둘레처럼 클리닉에서 특히 강조하고 싶은 항목을 검수 UI에서 체크박스로 지정)
  - **변화량 자동계산**: 사람이 입력하지 않음 — `시작`과 `마지막` 값에서 숫자를 파싱해 자동으로 차이를 계산하고 부호(+/-/±)와 단위를 붙여 표시 (예: "67.3kg" → "59.2kg" ⇒ "-8.1kg"). PPT 생성 로직(`generate_ppt.py`)에 이미 구현됨
  - **목표치 컬럼**: 연분홍 배경으로 시각적으로 구분
  - 항목 목록(기본 12개, 병원 요청시 조정 가능): 체중, 신장, 체지방량, 골격근량, 체지방률, BMI, 복부지방률, 복부둘레, 엉덩이둘레, 가슴둘레, 우측상완둘레, 우측대퇴둘레
- **QA 필수 절차** (매 생성마다):
  1. `markitdown`으로 콘텐츠 확인 (슬라이드 수, 텍스트 누락)
  2. `scripts/office/validate.py`로 파일 무결성 검증
  3. PDF 변환 후 이미지로 시각 QA (최소 첫 슬라이드, 5·15번 대각선 슬라이드, 마지막 표 슬라이드)

---

## 6. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| 처리 시간 | 환자 1인(32~64장) 기준 전처리~PPT생성 전체 5분 이내 목표 |
| 재작업성 | 크롭/분류 재조정 시 원본 재업로드 없이 저장된 `raw_path`+`crop_box`로 재처리 가능 |
| 에러 처리 | 포즈 미검출, 구도 중복/누락 시 검수 UI에서 명확한 경고 표시 |
| 동일 환자 재생성 | 파일명이 생성일자로 자동 구분되어 기존 파일 덮어쓰지 않음 (누적 저장) |
| 로깅 | 각 처리 단계(분류/크롭/생성) 결과를 로그 파일로 기록 (디버깅용) |

---

## 7. 개발 마일스톤 (제안)

1. **M1 — 전처리 프로토타입**: 포즈검출 + 구도분류 + 수평조정, 샘플 이미지로 정확도 검증 (스크립트 단위, UI 없이)
2. **M2 — 크롭 모듈**: 가이드 오버레이 포함 크롭 기능, 좌우 동기화
3. **M3 — 백엔드 API 골격**: FastAPI 라우터 구성, M1·M2 로직을 엔드포인트로 노출, 세션 상태 저장
4. **M4 — 프론트엔드 화면1~3**: 업로드 → 분류확인 → 크롭조정까지 연결 (React + API 연동)
5. **M5 — 프론트엔드 화면4~5 + PPT 생성 통합**: 매칭확인, 체성분입력, `generate_ppt.py` 연결, 진행상태 폴링
6. **M6 — 전체 파이프라인 통합 테스트**: 업로드~PPT생성 End-to-End, 표준/장기 모드 모두 검증

---

## 8. 미결정 사항 (개발 중 확인 필요)

- [ ] 구도별 디폴트 크롭 사이즈(px 또는 비율) 정확한 수치
- [ ] 정면/후면 오분류 시 보정 로직 고도화 여부 (임계값 튜닝만으로 충분한지)
- [ ] 체성분 수치 입력을 수기 입력으로 할지, 엑셀 업로드로 받을지
- [ ] `CropCanvas`의 드래그 크롭을 어떤 라이브러리로 구현할지 (react-image-crop 등 검토)
- [ ] 로컬 실행 방식: `npm run dev` + `uvicorn` 개별 실행 vs. 단일 실행 스크립트(Electron/Tauri 패키징 여부)

> **DB 불필요 확인**: 이 서비스는 환자 이력을 조회/검색하는 시스템이 아니라 방문 1건당 사진→PPT를 뽑아내는 일회성 처리 도구다. 따라서 SQLite 등 영구 데이터베이스는 두지 않는다. 필요한 저장은 (1) 최종 산출물(PPT·크롭사진)의 로컬 파일 저장, (2) 검수 위저드 진행 중의 휘발성 세션 상태(서버 메모리 또는 임시 JSON 1개, 생성 완료 후 폐기 가능) 두 가지뿐이다.
